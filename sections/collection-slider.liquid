{% assign col = collections[section.settings.collection].products %}
<div class="loaderEffect">
  <img src="{{ loader.png | asset_url }}" />
</div>
<div class="collectionSection">
  <div class="collection-header">
  <h1 class="collectionTitle">{{ section.settings.sectionTitle }}</h1>
    <a href="{{ collections[section.settings.collection].url }}" class="btnAll">View All</a>
  </div>
  <div class="collectionList">
    {% for colPro in col %}
      <div class="collection-product">
        <div class="collection-image">
          <img src="{{ colPro.images[0] | img_url: '300X300'}}" />
        </div>
        <div class="collection-content">
          <div class="product-title">{{ colPro.title }}</div>
          <div class="product-price">{{ colPro.price | money }}</div>
          <div class="product-button">
            {% if colPro.variants.size > 1 %}
              <a href="{{ colPro.url }}" class="btnPro optionsVar">Select Options</a>
            {% else %}
              <a href="javascript:void(0)" class="btnPro optionAdd" varid="{{ colPro.variants.first.id }}">Add to Cart</a>
            {% endif %}
          </div>
        </div>
      </div>
    {% endfor %}
  </div>
</div>
{% schema %}
  {
    "name": "Collection Slider",
    "settings": [
      {
        "type": "text",
        "id": "sectionTitle",
        "label": "Title"
      },
      {
        "type": "collection",
        "id": "collection",
        "label": "Collection"
      }
    ],
  "presets": [
    {
      "name": "Collection Slider",
      "category": "collection"
    }
  ]
  }
{% endschema %}